{
  "name": "N-age-warzone-Gacha!",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "axios": "^1.3.2", 
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "serve": "^14.0.1"  // เพิ่ม serve ใน dependencies
  },
  "scripts": {
    "start": "serve -s build",  // ใช้ serve เพื่อให้บริการไฟล์ static
    "build": "react-scripts build",  // คำสั่ง build ปกติ
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  }
}
